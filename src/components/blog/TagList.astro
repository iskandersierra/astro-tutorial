---
import TagListView from "./TagListView.astro";
import { getTagList } from "../../pages/tags/utils";

const { tags } = Astro.props as { tags?: string[] };
const tagList = await getTagList({
    filteredByTags: tags,
});
---

<TagListView tags={tagList} />
